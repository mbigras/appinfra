---
groups:
  - name: node
    rules:
      - alert: NodeExporterDown
        expr: up{job="node"} == 0
        for: 1m
        labels:
          severity: "critical"
        annotations:
          summary: "Node Exporter is down on {{ "{{" }} $labels.instance {{ "}}" }}"
          description: ""
